import AppRoutes from './routes/AppRoutes';

/**
 * App.jsx
 * 
 * Root application component.
 * Responsible for loading the routing layer.
 * 
 * App → AppRoutes → Individual Pages
 */
const App = () => {
  return <AppRoutes />;
};

export default App;
